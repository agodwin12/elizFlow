# docker-compose.yml
version: "3.9"

services:
  backend:
    build: .
    container_name: elizflow_backend
    ports:
      - "11000:11000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped